 /* while loops
 var num = 0;
 while (num < 100) {
     num += 1;
     console.log(num);
 }
*/

// for loop
for (let num = 0; num <99; num++) { // is the same as while (num) but is a better way to write and read the code
    console.log(num);
}